# Channel は upstream cuda-oxide の rust-toolchain.toml に追従する
# (cuda-oxide が ABI-unstable な rustc internal API を直接使うため、
#  channel ずれは link-time fail を引き起こす)。
[toolchain]
channel = "nightly-2026-04-03"
components = ["rust-src", "rustc-dev", "rust-analyzer", "clippy", "llvm-tools"]
